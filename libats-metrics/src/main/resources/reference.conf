ats {
  metricsReporter {
    reportMetrics = true
    host = "localhost"
    host = ${?INFLUXDB_HOST}
    port = 8086
    port = ${?INFLUXDB_PORT}
    database = "services"
    database = ${?METRICS_DB}
    serviceName = ${serviceName}
    instanceId = ${instanceId}
    interval = 5s
  }
}
