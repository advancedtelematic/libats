database = {
  driver = "org.mariadb.jdbc.Driver"
  catalog = "libats_slick"
  url = "jdbc:mariadb://localhost:3306/libats_slick"
  url = ${?DB_URL}
  properties {
    user = "libats_slick"
    user = ${?DB_USER}
    password = "libats_slick"
    password = ${?DB_PASSWORD}
  }
}
